@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Redefinir chave do autenticador";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Se redefinir a chave do autenticador, a sua aplicação autenticadora não funcionará até que a reconfigure.</strong>
    </p>
    <p>
        Este processo desativa a autenticação em dois passos (2FA) até verificar a sua aplicação autenticadora.
        Se não completar a configuração da sua aplicação autenticadora, poderá perder o acesso à sua conta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Redefinir chave do autenticador</button>
    </form>
</div>