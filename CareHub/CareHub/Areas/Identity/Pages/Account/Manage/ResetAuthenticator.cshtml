@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Redefinir chave de autenticação";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Se redefinir a sua chave de autenticação, a sua aplicação de autenticação não funcionará até ser reconfigurada.</strong>
    </p>
    <p>
        Este processo desativa a autenticação de dois fatores (2FA) até que verifique a sua aplicação de autenticação.
        Se não completar a configuração da sua aplicação de autenticação, poderá perder o acesso à sua conta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Redefinir chave de autenticação</button>
    </form>
</div>