@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Gerar códigos de recuperação para autenticação em dois passos (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Guarde estes códigos num local seguro.</strong>
    </p>
    <p>
        Se perder o seu dispositivo e não tiver os códigos de recuperação, perderá o acesso à sua conta.
    </p>
    <p>
        Gerar novos códigos de recuperação não altera as chaves usadas nas aplicações autenticadoras. Se desejar alterar a chave
        usada na aplicação autenticadora, deve <a asp-page="./ResetAuthenticator">redefinir as suas chaves autenticadoras.</a>
    </p>
</div>
<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Gerar códigos de recuperação</button>
    </form>
</div>